# 12. 未决清单

> 状态：草案锁定  
> 规则：本文件专门承载未确认细节。这里的内容不是结论，而是待你逐项核对的清单。

| ID | 待确认事项 | 为什么重要 | 当前默认假设 |
|---|---|---|---|
| Q-001 | 项目正式名称、产品名、对外发布名是否统一 | 影响文档、包名、界面与版本体系 | 暂用当前仓库名与规划名 |
| Q-002 | Go 工具链主版本冻结到哪个小版本 | 影响构建、语法、依赖与发布稳定性 | 先按当前稳定主线，待实现前冻结 |
| Q-003 | Windows/macOS 是否要求完整网关模式，还是仅 Agent + 本地隧道/策略/诊断 | 影响平台能力边界和系统集成范围 | 先按 Agent + UI 为主，Linux 承担完整网关优先职责 |
| Q-004 | Linux 上 DHCP、DNS、透明代理是否作为首版硬要求 | 影响首版网络职责范围 | 默认保留并作为 Linux 网关能力 |
| Q-005 | 桌面 UI 是否需要系统托盘、开机自启、通知中心集成 | 影响三平台交互体验与打包方式 | 默认需要 |
| Q-006 | 客户端安装包格式如何定 | 影响分发与升级流程 | 暂按各平台原生安装包规划 |
| Q-007 | 自动更新是否首版纳入 | 影响发布管线与回滚设计 | 默认纳入受控更新能力 |
| Q-008 | 更新是否要求签名校验与回滚包 | 影响安全与恢复能力 | 默认要求 |
| Q-009 | 控制面对外 API 是否只用 HTTP/JSON，还是要同步提供 gRPC | 影响控制台与自动化接入方式 | 当前文档采用 HTTP/JSON + 内部 gRPC 的混合方案 |
| Q-010 | 设备认证是否采用证书 + token 的混合方式 | 影响登录、设备接入和权限分层 | 默认证书为主、token 为辅 |
| Q-011 | 日志与遥测的保留周期是多少 | 影响合规、成本与排障能力 | 默认需要单独制定留存策略 |
| Q-012 | 是否允许上传更细粒度诊断日志 | 影响隐私与合规边界 | 默认允许，但必须脱敏 |
| Q-013 | Billing/计费是否进入首版正式范围 | 影响控制面复杂度 | 当前仅保留后续企业级能力位置 |
| Q-014 | `sing-box` 在首版中是正式依赖还是仅参考能力来源 | 影响协议栈实现路径 | 默认可作为可复用能力来源，但不写死为唯一实现依赖 |
| Q-015 | 出口池初始规模与区域配比是否要固定 | 影响运维容量与调度策略 | 默认不固定，按可插拔池管理 |
| Q-016 | 首版高可用是否只做到节点级，还是要加区域级冗余 | 影响部署成本与复杂度 | 当前只承诺节点级故障切换 |
| Q-017 | 具体 SLO、故障切换时延和验收阈值是多少 | 影响测试标准与发布门槛 | 待在测试标准中冻结 |
| Q-018 | Web 控制台的首版页面清单是否还要增加更多运维视图 | 影响控制面工作量 | 当前只定义基础页面 |
| Q-019 | 手机端后续是原生 App 还是只复用 API 层 | 影响长期架构 | 当前只保留 API 和模型复用 |
| Q-020 | 是否需要对接外部身份系统 | 影响租户与权限设计 | 默认先自建身份体系 |
| Q-021 | 客户端 Agent 与桌面 UI 之间的本机通信是否统一采用 gRPC、HTTP/JSON、Unix domain socket、Named Pipe，还是按平台分别实现 | 影响 UI/Agent 解耦和本地鉴权模型 | 默认先按本机通信层抽象，平台实现待定 |
| Q-022 | 管理员与运维人员的认证方式是否采用用户名密码 + MFA、OIDC/SSO，还是完全自建账号体系 | 影响 Web 控制台与 API 的统一身份体系 | 默认先自建账号体系，外部身份后续评估 |
| Q-023 | 控制面租户层级是否固定为组织 / 项目 / 站点 / 设备 / 节点，还是需要其他层级 | 影响数据模型和权限边界 | 默认先按组织-站点-设备-节点抽象 |
| Q-024 | 设备首轮入网/注册流程如何完成：人工审批、邀请码、一次性令牌还是证书预置 | 影响安全与交付流程 | 默认需要受控注册流程 |
| Q-025 | 自建 CA 的层级结构、根证书离线保管方式和中间证书轮转周期如何定 | 影响证书生命周期和安全边界 | 默认采用根 CA + 中间 CA 的分层模型 |
| Q-026 | 证书吊销分发采用 CRL、OCSP 还是控制面推送黑名单 | 影响节点离线时的吊销可见性 | 默认由控制面统一分发，细节待定 |
| Q-027 | 客户端、边界节点和控制面的日志脱敏规则和字段白名单如何冻结 | 影响遥测可用性和隐私边界 | 默认按最小必要字段上报 |
| Q-028 | 日志和指标的保留周期、冷热分层和归档策略如何定 | 影响成本、合规与排障能力 | 默认需要单独保留策略 |
| Q-029 | 控制面与节点之间的配置下发协议是否需要版本回滚、幂等和签名校验 | 影响故障恢复和防篡改 | 默认要求版本化与签名校验 |
| Q-030 | 入口协议选择的阈值和打分模型如何定义（RTT、丢包、抖动、UDP 可用性权重） | 影响自动切换稳定性 | 默认采用可配置评分模型 |
| Q-031 | 链路类型识别是基于主动探测、被动观测还是混合策略 | 影响切换准确率与探测成本 | 默认采用混合策略 |
| Q-032 | 协议切换的冷却时间、回退条件和最小中断窗口目标是多少 | 影响用户可感知切换体验 | 默认需要明确回退窗口 |
| Q-033 | 出口策略中域名、IP、ASN 的冲突优先级和覆盖规则如何定义 | 影响策略命中一致性 | 默认需要统一优先级表 |
| Q-034 | AI、视频、企业业务、国内回源等流量分类的初始规则集和更新方式如何管理 | 影响默认路由策略 | 默认由控制面集中下发 |
| Q-035 | 海外出口池的健康检查频率、失败次数阈值和摘除/恢复策略如何定 | 影响故障切换与容量利用 | 默认按健康检查驱动池管理 |
| Q-036 | 海外边界层的限速、鉴权和入口调度的具体服务拆分是否需要独立微服务 | 影响控制面与边界节点边界 | 默认逻辑可拆分，部署形态待定 |
| Q-037 | Windows 平台是否要求 WFP 深度集成、服务安装和驱动签名流程 | 影响首版平台工作量 | 默认需要平台专用集成方案 |
| Q-038 | macOS 平台是否要求 Network Extension、系统扩展或其他系统能力 | 影响平台工作量与分发方式 | 默认需要平台专用集成方案 |
| Q-039 | Linux 网关能力是否要求 systemd、nftables、iproute2、WireGuard 模块的明确版本基线 | 影响部署可重复性 | 默认需要版本基线待定 |
| Q-040 | 桌面 UI 是否需要系统托盘、开机自启、通知中心和自检入口 | 影响三平台交互一致性 | 默认需要，细节待确认 |
| Q-041 | 首版控制台页面的最小集合是否就是总览、连接状态、节点、策略、诊断、日志、证书、更新、设置 | 影响产品范围 | 默认以现有清单为最低集 |
| Q-042 | Web 控制台与外部 API 的认证授权是否统一使用同一套角色/权限模型 | 影响权限实现和审计 | 默认需要统一模型 |
| Q-043 | 多租户隔离的技术边界是逻辑隔离、库级隔离还是物理隔离 | 影响数据库和部署方案 | 默认先按逻辑隔离设计 |
| Q-044 | 控制面数据库、缓存和任务协调的 HA 与备份恢复目标值如何定 | 影响运营韧性 | 默认需要单独定义 RPO/RTO |
| Q-045 | 构建与发布是否要求可重现构建、签名发布和版本注入的具体规范 | 影响交付链安全 | 默认需要纳入正式构建规范 |
| Q-046 | 协议、策略、权限、UI 和平台适配各自的测试覆盖门槛如何冻结 | 影响正式版门禁 | 默认按测试标准逐步冻结 |
| Q-047 | 跨平台兼容测试是否要求在 Linux、Windows、macOS 的全部 x86_64/ARM64 组合上跑通 | 影响发布矩阵 | 默认按测试标准的全矩阵规划 |
| Q-048 | 具体 SLO、故障切换时延、重试次数和验收阈值如何设置 | 影响测试标准与发布门槛 | 默认待在测试标准中冻结 |
| Q-049 | 是否需要对接外部身份系统、IM/告警系统或工单系统 | 影响控制面集成边界 | 默认先不对接，保留接口 |
| Q-050 | 项目正式名称与对外产品名是否需要与仓库名分离 | 影响文档、UI 和包名 | 默认暂用当前名称 |
